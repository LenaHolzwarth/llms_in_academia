\chapter{Methods}\label{sec:methods}
\section{Dataset and Preprocessing}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/papers_journals_overview.png}
    \caption{Overview of the PMC dataset after preprocessing. Panel \textbf{a} shows the number of papers in PMC published in each month starting in 2017 (1,194,287 in total). The mean number of papers published per month is 11,085, with a clear upwards trend. Panel \textbf{b} shows the distribution of journals after preprocessing. All journals with less than 4000 papers are listed as ``other'' for reasons of legibility. The journal \emph{Proceedings of the National Academy of Sciences of the United States of America} was abbreviated to ``NAS proceedings''.}
    \label{fig:papercount}
\end{figure}
\subsection{Pubmed Central}
Pubmed Central (PMC) is a collection of open-access biomedical literature curated by the U.S. National Institutes of Health's (NIH) National Library of Medicine \cite{pmc}. 
It provides machine-readable versions of the articles in XML format, which can be accessed individually or downloaded in bulk.
There are three open-access subsets available for bulk download, each  with different licensing: \emph{commercial}, \emph{non-commercial} and \emph{other}.
The latter set includes non machine-readable licenses and was therefore not used.
PMC publishes a baseline twice a year containing all papers in the collection up to shortly before the baseline's release date. 
Incremental updates are published daily in separate files.
When a new baseline is released, the previous baseline and all incremental files are replaced.
Here, we used the baseline published on January 23, 2026 containing papers dated December 31, 2025 and before.\footnote{Since work on this thesis started earlier, initial analyses used the previous baseline released June 26, 2025. These were repeated with the January 2026 baseline, so that everything reported here is based on the newer baseline. Exceptions to this are clearly marked in the text.}
The daily updates published after the baseline were not considered.
The baseline was downloaded via FTP, which is no longer supported, but is available via other channels\footnote{https://ftp.ncbi.nlm.nih.gov/pub/pmc/deprecated/oa\_bulk/}.
\\
Figure \ref{fig:papercount} offers an overview of the dataset after preprocessing.
It shows that the top three journals with most papers in PMC are \emph{Scientific Reports} (8.1\%), \emph{Nature Communications} (4.7\%) and \emph{PLoS ONE} (3.9\%), all of which are multidisciplinary publications covering a wide range of topics in the natural sciences. 
\emph{Scientific Reports} and \emph{Nature Communications} are both from the Nature Research family.
The smaller publications are more directly related to PMC's focus on biomedical research, such as the \emph{International Journal of Molecular Sciences} (1.9\%) or \emph{Frontiers in Immunology} (1.6\%).
Since PMC also includes preprints, some of the ``journals'' are preprint platforms.
For example, 1.4\% of papers are from bioRxiv.
There are also preprints from medRxiv, arXiv and Research Square.
PMC doesn't include all papers from preprint platforms, only those that have received NIH funding.
Overall, there are 22,470 preprints in the dataset after preprocessing, making up 1.8\% of all papers.
Once a preprint is published in a journal that is part of PMC, the preprint is not replaced, such that two versions of the same paper can exist in PMC. 
This is not ideal, because it means that some papers are weighed double in the analysis.
However, not all preprints will get published in an open-access journal, which means that at most 1.8\% of the dataset are sampled twice.
\\\\
\subsection{XML parsing}
From the XML files in the baseline, each corresponding to one paper, the following information was extracted:
\begin{itemize}
    \item \textbf{Article type}: specifies whether the article is a research paper, a review, a comment, etc.
    \item \textbf{Date}: refers to the date of publication, not the date of incorporation to PMC
    \item \textbf{Language}: only specified for a minority of papers, but is inferred later during preprocessing 
    \item \textbf{Country}: country of first affiliated author, or first affiliated institution. Only available for 80.2\% of entries (after preprocessing)
    \item \textbf{PMC-ID}: unique identifier for every paper in PMC. Note that different versions of a paper (for example a preprint and its published version) are assigned different PMC-IDs.
    \item \textbf{PMID}: unique identifier for PubMed, not available for every paper in PMC
    \item \textbf{Journal name}
    \item \textbf{Title}
    \item \textbf{Abstract}
    \item \textbf{Sections}
    \item \textbf{Section titles}
\end{itemize}
The sections and section titles are parsed from the main body of the XML file.
In general, individual sections are tagged as \texttt{$<$sec$>$} and assigned a title.
This is important for being able to compare different sections with each other, for which we need as many papers as possible with similar sections.
However, since there is no standardized format for the files, some sections are untitled or there are no section tags at all.
In particular, many files have section names for all sections except the introduction, or don't embed the introduction in a separate section tag. 
Therefore, if a file has more sections than section titles, and the first section is unnamed, it is assumed to be the introduction.
Any other untitled sections are discarded.
Similarly, if there is text at the beginning of the body outside of a sec tag, it is assumed to be the introduction. 
This heuristic is not ideal, because the unnamed text contains abbreviations or keywords instead of the introduction in some cases, but since the Introduction is among the sections this study wants to focus on, it strongly increases the number of usable papers.
\\\\
\subsection{Preprocessing}
After parsing all relevant information from the XML files, the extracted papers were filtered to fit the required criteria.
While some XML files specify the paper's language, this is not the case for the majority.
Since the focus here is on English text, we determine each paper's language using polyglot's detector module\footnote{https://polyglot.readthedocs.io/}.
Every non-English paper is discarded.
The dates of each paper are standardized to DD-MM-YYYY format. 
Some entries only specify the month (e.g. "Feb 2025") or season of the year (e.g. "Fall 2025"), in which case the first of the month or start of the (meteorological) season are used as dates.
Papers without date are removed.
Since we want to compare LLM-use across different sections, it is important to have as many papers with comparable section structure as possible.
To achieve this, in a first step the section names were manually standardized by collecting the most frequent section names and mapping them to their standardized version.
For example, ``experimental results'', ``findings'' or ``main results'' would be mapped to ``results''.
If a paper has multiple sections with the same standardized title, they are concatenated.
For example, ``data'' and ``experimental methods'' would first both be renamed as ``methods'' and then combined into a single methods section.
Originally, we planned to investigate different article types and article structures, but in the end we focused only on research articles that have exactly the sections introduction, methods, results, and discussion, since papers with this article structure were the majority in the dataset. 
Conversely, all papers with article types other than ``research-article'' or with more, fewer or different sections were discarded.
Finally, all sections, including the abstract, were required to have a length of at least 250 characters.
These preprocessing steps reduce the dataset from 7,125,722 to 1,602,404 papers, 1,194,287 of which are dated 2017 or later 
(2017 is the earliest time used in our analyses).
\\
While these preprocessing steps strongly reduce sample size, the resulting dataset can be used to compare LLM-usage in the abstract to LLM-usage in the entire body of the paper, as well as to compare different sections within the body with each other.
For this purpose, all analyses detailed in the following sections were performed on the abstract, introduction, methods, results and discussion of the paper, as well as the full paper, which refers to the four sections from the body without the abstract.
With the finished dataset, we can turn to the methods used to estimate its proportion of LLM-generated text.

\section{Excess Frequency and LLM-use lower bound estimates}
\begin{figure}[htbp]
    \centering
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/these_freqs_time.png}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/these_usage_time.png}
    \end{minipage}
    \caption{Example of using excess frequency to determine lower bounds for LLM-use. Panel \textbf{a} shows the frequency for the word ``these'' in PMC abstracts. The observed frequency $q$ (solid line) diverges strongly from the expected frequency $\hph$ based on the years before the release of ChatGPT. This difference corresponds to the frequency gap $Delta$ in panel \textbf{b} (dashed line), which can be interpreted as a lower bound on LLM-use. A stronger lower bound $\beta_{lb}$ is also possible (solid line), which increases the estimated lower bound by over 0.1.}
    %comment: change title  and axes to lower bound, add letters,
    \label{fig:these}
\end{figure}
Excess frequency, referring to the difference between the observed and predicted frequencies for some word $w$, can be thought of as an intuitive lower bound for LLM-use.
It was introduced by Kobak et al. \cite{Kobak2025}, upon whose work this thesis expands.
To compute it, we first determine the observed frequency of $w$.
We use scikit-learn's binary CountVectorizer to count the number of papers in each month or year in which $w$ occurs \cite{scikit-learn}.
Because it is a binary count, it doesn't matter how often $w$ appears in each paper.
The vectorizer only considers words with four or more letters, all of which must be part of the English alphabet.
When counting word occurrence, lemmatization is an important consideration: one has to determine whether to treat different inflections of $w$ as the same or as individual words.
With lemmatization, it makes no difference if a paper contains ``delves'', ``delved'' or ``delving'', they are all treated as an instance of ``delve''.
It is not immediately clear if lemmatization is helpful in this use-case.
As has been mentioned in Section \ref{sec:intro}, LLMs prefer certain words.
This could imply that LLMs use all inflections of a word at a higher frequency, or that they have a strong preference for that word in a specific grammatical context (e.g. third-person singular), while using every other inflection at the same frequency in which it occurs in human-written text.
As will be detailed in Section \ref{subsec:wordlist}, for the overall LLM-usage estimates, this does not matter. 
For the plots showing results for individual words, such as Figures \ref{fig:these} and \ref{fig:potential_delve}, all word variants are included in the counts.
The frequencies $q$ are computed by dividing the monthly or yearly counts by the total number of papers in each respective time frame.
While monthly intervals offer a more detailed look into the data, for some computations it makes more sense to do them in yearly intervals to save resources.
On a conceptual level, the observed frequencies $q$ are an estimate for the probability of a positive outcome in the Bernoulli trial of whether a word is present in some paper.
For dates prior to the release of ChatGPT in November 2022, marking the beginning of easily accessible LLMs, $q$ corresponds to the frequency of a word in human-written text, $p_{human}$.
After the widespread availability of Chatbots, the observed word frequency can be thought of as the product of a mixture distribution between the probability of a word to appear in human-authored and LLM-assisted text, respectively: 
\begin{equation}
    q = 
    \begin{cases}
        p_{human} & \text{for papers before 11-2022}\\
        (1-\beta) \cdot p_{human} + \beta \cdot p_{LLM} & \text{for papers after 11-2022}\\
    \end{cases}
\end{equation}
The mixture component $\beta$ in the lower case indicates the proportion of papers that were written with the help of LLMs.
This is the LLM-use rate that this work aims to investigate.
We can transform this equation to obtain an expression for $\beta$:

\begin{equation}\label{eq:beta}
\begin{split}
    q &= (1-\beta) \cdot p_{human} + \beta \cdot p_{LLM}\\
    \beta \cdot (p_{LLM}-p_{human}) &= q -  p_{human}\\
    \beta &= \frac{q -  p_{human}}{p_{LLM}-p_{human}}
\end{split}
\end{equation}
\\
While $p_{human}$ can be estimated by the observed $q$ in the time span before November 2022, there is no direct way to do so after the release of ChatGPT.
However, if we assume that human usage of words follows linear trends and is independent of LLM-use, we can use the values before 2022 to model $\hph$ using linear regression.
Since we are interested in both the monthly and yearly trends, we fit the regression either on the five yearly frequency values from 2018 to (including) 2022, or on the 60 monthly frequencies from November 2017 to October 2022.
For the yearly data we include 2022 in the train set because, considering the time it takes to write a paper, and the time it took the new technology to gain traction, it is unlikely that many papers published in December 2022 were written with the help of LLMs.
Replacing the unknown $p_{human}$ with the estimate $\hph$ in equation \ref{eq:beta}, one issue remains: there is no direct way to estimate $p_{LLM}$ from the observed frequencies.
Other estimation methods solve this problem by using ground-truth LLM-generated text to determine $p_{LLM}$ (see Section \ref{subsec:freq_model} for details).
Here, we choose a different approach by determining a lower bound for $\beta$ that doesn't rely on $p_{LLM}$:
\begin{equation}\label{eq:delta}
    \begin{split}
        \beta &= \frac{q -  \hph}{p_{LLM}-\hph}\\
        \beta &\ge q - \hph =\Delta
    \end{split}
\end{equation}
This is the excess frequency measure introduced by Kobak et al. and denoted $\Delta$ \cite{Kobak2025}.
Intuitively, if a word appears in $70\%$ of papers in 2025, but we predict humans to use this word in only $60\%$ of instances, the $10\%$ gap must be accounted for as having been written by LLMs. 
Note that this metric will lead to a strong under-estimation of the actual LLM usage, as illustrated in Figure \ref{fig:intution_estimates}.
This is because a human-use frequency of $60\%$ will only translate to $60\%$ of observed use, if all texts in the sample are in fact written by humans ($\beta=0$).
In reality, some papers were written with the help of LLMs, such that the fraction of word-occurrence that can be explained by human usage is not $60\%$ of all papers, but $60\%$ of the papers with human authorship.
This under-estimation is necessary, because $\beta$ is unknown and the most conservative assumption is $\beta=0$.
This makes $\Delta$ an accurate, but relatively weak lower bound.
A stronger lower bound, denoted $\beta_{lb}$ is also possible:
\begin{equation}\label{eq:beta_hat}
    \begin{split}
        \beta &= \frac{q -  \hph}{p_{LLM}-\hph}\\
        \beta &\ge \frac{q -  \hph}{1-\hph} = \beta_{lb}
    \end{split}
\end{equation}
%comment: write a paragraph about limiting factors/behaviors of this formula
%comment: consider case for p_llm = 1
Here, we replace the unknown $p_{LLM}$ with its upper bound 1, thereby increasing the denominator, which creates a lower bound for $\beta$.
Note that in cases where $p_{LLM}=1$, such that a word or group of words appear in every LLM-assisted text, the lower bound $\beta_{lb}$ is identical to $\beta$.
There are certain limitations of this formula.
First, it requires $p_{LLM}$ to be strictly larger than $\ph$, otherwise the lower bound will be zero or negative.
This disqualifies words that occur less frequent post-2022, because they are less likely to be used in LLM-generated text, from being useful marker words for this metric.
Second, as stated before, $\beta_{lb}$ depends on the assumption that $\hph$ accurately predicts $\ph$ after the release of ChatGPT.
This requires $\ph$ to follow a linear trend and to be independent of $p_{LLM}$, meaning that human authors don't let their writing style be influenced by LLM word choices.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{../results/plots/misc/intuition_estimate_v3.png}
    \caption{Illustration of the frequency gap as a lower bound for LLM-use and why it (strongly) underestimates true LLM-use. In this example, we assume an underlying distribution that is shown in the top row. Humans are assumed to use a word in 60\% of texts, so $\ph=0.6$. This proportion is shown by the blue bar. We further assume that LLMs use a word in 80\% of texts ($\pl=0.8$), which is indicated by the green bar. LLM-use $\beta$ is assumed to be 0.5, meaning that half of the texts in the sample are LLM-assisted. When sampling from this distribution in the second row, we observe that in total, 70\% of texts use this word. This observation is the word frequency $q$ that we measure for PMC. In this example, we know that the 70\% of overall usage can be explained by a combination of humans and LLMs using this word. In particular, $\ph\cdot(1-\beta)= 60\% \cdot 0.5 = 30\%$ is explained by humans using the word, and $\pl\cdot\beta= 80\% \cdot 0.5 = 40\%$ is caused by LLMs using the word. However, when trying to estimate LLM-use, we only have access to $\ph$ (indirectly, via regression). Since we don't know $\beta$, the most conservative estimate is $\beta=0$, i.e. no texts are written with the help of LLMs. This means that we would expect $\ph\cdot(1-\beta)= 60\% \cdot 1 = 60\%$ of papers to include a term if all texts were written by humans, so the remaining $\Delta = q - \ph = 10\%$ of word occurrences must have been written by an LLM. Therefore the rate of LLM-use must at least be 10\%. This method of lower-bounding the LLM-use thus underestimates the true proportion of LLM-assisted text by 40 percentage points.} 
    \label{fig:intution_estimates}
\end{figure}

\section{Grouped word frequencies}\label{subsec:wordlist}
\begin{figure}[htbp]
    \centering

    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/potential_freqs_time.png}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/potential_usage_time.png}
    \end{minipage}

    \medskip

    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/delve_freqs_time.png}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/delve_usage_time.png}
    \end{minipage}

    \caption{Examples of words with noticeable frequency changes in abstracts from PMC after the release of ChatGPT. The left column shows the word's observed frequency $q$, the right column shows the resulting lower bound estimates. While ``potential'' has a strong increase in frequency (panel \textbf{a}), this only yields a lower bound estimate of below 10\% (panel \textbf{b}). ``Delve'' has a very strong increase in frequency up to mid-2024, followed by a steep decrease (panel \textbf{c}), which is mirrored in the lower bound estimates shown in panel \textbf{d}.}
    %comment: Fix title for delve estimates, add letters
    \label{fig:potential_delve}
\end{figure}

While single words are useful to illustrate the concept of excess frequency, they are suboptimal as a basis for an accurate estimate of overall LLM-use.
The reason for this is illustrated in Figure \ref{fig:potential_delve}: while LLMs might use a word such as ``potentially'' at a higher frequency than humans would, ``potentially'' doesn't fit in every context, so there will potentially be a lot of LLM texts not containing it.
Accordingly, the lower bound for ``potential'' is much lower than that for the relatively frequent word ``these'' in Figure \ref{fig:these}.
Also, the observed frequencies of some words might be unreliable.
The word ``delve'', used as the titular example of excess vocabulary by Kobak et al., has experienced a sharp decline in popularity starting around mid-2024, when it was widely reported as an example of LLM-generated language \cite{Geng2025}.
To protect against these fluctuations, Kobak et al. replace individual words with word lists.
The method of computing the frequencies doesn't change, but instead of counting in how many papers a single word appears, they count in how many papers at least one of the words in the list appears.
The most systematic way to create a list of words indicative of LLM-use is to use a data-driven approach, which doesn't require a manually selected list.

\subsection{Excess words for 2024 PubMed}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{../results/plots/misc/exess_words_18_24.png}
    \caption{Frequency ratios for all words in PubMed abstracts in 2018 (panel \textbf{a}) and 2024 (panel \textbf{b}). The frequency ratio is computed by dividing the frequency in the target year by the predicted frequency of the target year, based on linear predictions from two and three years prior. All words above the threshold (dashed line) are marked as excess words, i.e. words with notable change in frequency. Excess words are manually labeled as \emph{content words} (blue dots) and \emph{style words} (orange dots). In 2024, there are many more excess style words than in 2018. The Figure is taken from Kobak et al. \cite{Kobak2025}}
    \label{fig:excess_words_18_24}
\end{figure}
Kobak et al. determine the excess words for 2024 by computing the frequency gap and the frequency ratio for each word in their dataset.
The frequency gap is the same metric as $\Delta$ in Equation \ref{eq:delta}, although they only use the yearly frequencies of 2021 and 2022 to predict $\hph$.
The frequency ratio is computed as $\frac{q}{\hph}$.
An excess word is defined as any word with frequency gap or frequency ratio above a certain threshold.
The thresholds were chosen in such a way that the majority of words in texts from the years 2012 - 2022 were excluded
(the gaps for previous years were also computed by comparing observed values to $\hph$ computed based on two and three years prior).
For example, in Figure \ref{fig:excess_words_18_24}, the frequency ratio of all words in Pubmed abstracts from 2018 are compared to those of 2024. 
There are only a few words with frequency ratios higher than the threshold in 2018, whereas there are many in 2024.
Using both metrics as inclusion criteria serves to increase the number of excess words.
For instance, a word whose usage increases from 0.0001 to 0.001 will have a relatively small gap but a large ratio, while a word whose prior frequency of 0.3 rises to 0.4 has a large gap with a relatively small ratio. 
Both changes mark a notable difference in usage and should therefore be candidates for marker words.
All words above the threshold were then manually annotated as \emph{style words} or \emph{content words}.
\emph{Content words} are all words related to the research topic of a certain paper. 
Their excess use can be attributed to new inventions or research trends.
Examples include ``RNN'' in 2018, ``Covid'' in 2020, and ``ChatGPT'' in 2024.
\emph{Style words}, on the other hand, are not related to research topics and could be found in any discipline: ``these'', ``potentially'', ``delve'', etc.
Because they are independent of the subject of research, and no previous year showed such a strong increase in the frequency of style words, it is reasonable to assume that their unexpectedly high frequency in 2024 by the large-scale introduction of LLMs into the academic writing process.
All style words with frequency gap or ratio gap above the respective thresholds in 2024 were selected as marker words for LLM-use.
This resulted in a list of 379 words, which are listed in the Appendix \ref{ap:wordlist}.
For simplicity, this list will be referred to as the excess word list from here on out, even though it could be more accurately referred to as a list of excess style words from 2024 Pubmed abstracts.

\subsection{Optimal word lists}
The excess word list can be used to create the optimal list of marker words to detect LLM-use.
It might be counter-intuitive to not just use the whole list: if any of the words can act as a marker for LLM-use, then using as many words as possible should yield the most accurate result.
The metric $\beta_{lb}$ yields the highest lower bound by getting $p_{LLM}$ as close to $1.0$ as possible, which would in turn move $\beta_{lb}$ closer to the true value $\beta$.
While $p_{LLM}$ isn't observable, it can be manipulated by increasing the number of words in the excess list.
The longer the list, the more likely it is for all LLM-assisted text to contain at least one of the words in it.
However, one has to keep in mind that all excess words also appear in human-written text.
A long list of excess words could thus result in $\hph = 1.0$, which leads to division by zero in Equation \ref{eq:beta_hat}.
A high value of $\ph$ is also suboptimal for using $\Delta$ as the lower bound, because the closer $\ph$ is to $1.0$, the smaller the potential gap.
This means that for both lower bounds, $\Delta$ and $\beta_{lb}$, a subset of the 2024 excess words needs to be determined.
Because brute-forcing the decision on an optimal list by trying all possible combinations is not feasible, Kobak et al. sort the excess words by their individual frequencies in 2024 (lowest to highest), and then use different cutoff values to create subsets of all words whose individual frequencies are below the cutoff. 
Each resulting list is used to compute the LLM-use estimate $\Delta$ and the highest $\Delta$ is used as the final estimate.

\subsection{Adaptation to PMC data}
Initially, we planned to repeat the previous authors' procedure to find excess words for the PMC dataset.
This turned out to be non-feasible, because the length of the full papers lead to finding a very large number of excess words for the different sections, which would all have to be manually annotated to filter them for style words.
Instead, we used the previous study's 2024 excess word list.
This is permissible because, first, PMC is a subset of the PubMed data used by Kobak et al., so the domain is similar, and second, the point of using style words is to have marker words that are domain-independent
(although it is likely that when searching for excess words in a different domain, such as news articles, the resulting list would be somewhat different).
The 2024 excess word list is not lemmatized, so that ``delving'' and ``delves'' are treated as different words.
Since we are not interested in the excess words on a semantic level and use them only as indicator of LLM-use, it does not matter if two words on the list are variations of the same verb, or if a third variation of the verb is not included.
\\
To get the optimal list for each section, we sort the 2024 excess words by their individual frequency in that section in 2024.
This leads to a somewhat different order for each section.
Like in the previous work, we then use different cutoff values to create subsets of excess words, which serve as a basis for computing the lower bound for LLM-use, $\beta_{lb}$.
We started with 12 cutoffs spaced evenly on a log-scale between 0 and 1 and later added intermediate values in the high-interest region around the maximum, resulting in 19 cutoffs.
The highest value of $\beta_{lb}$ is used as our estimate for $\beta$ and denoted $\hbet$.
However, in some cases, the maximum occurs at one of the highest cutoff value, leading to nearly the full excess word list being used for the estimate.
This results in values for $q$ and $\hph$ that are extremely close to 1, sometimes with $\hph$ being larger than $q$, which contradicts the assumption $p_{human} < p_{LLM}$.
As a consequence, both numerator and denominator of the fraction $\frac{q - \hph}{1-\hph}$ are very close to 0, which can lead to unstable behavior.
Figure \ref{fig:erratic} gives an example of this, showing smoothly increasing lower bounds for lower cutoffs and erratic behavior for the highest few.
For example, when using all excess words (corresponding to a cutoff of $1.0$), the metric estimates 100\% LLM-use in the years 2019, 2020, 2023 and 2025. 
This happens when $q=1.0$, which leads to the numerator and denominator of $\beta_{lb}$ being identical.
In theory, $q=1.0$ is a valid observation, which logically entails that $\beta=1.0$, if $\hph<1.0$.
The problem is that $q$ is an empirical estimate measured with some uncertainty.
Consider a situation where $q=1.0$ and $\hph=0.9998$, which leads to an estimate of $\beta_{lb}=\frac{q-\hph}{1-\hph} = \frac{1-0.9998}{1-0.9998}=1$.
If we minimally adjust $q$ to $q=0.9999$, the estimate is $\beta_{lb}= \frac{0.9999-0.9998}{1-0.9998}=0.5$ instead.
To avoid fluctuations like this, any cutoff that leads to estimates of $\hph \ge 0.999$ is excluded from optimization.
This removes all cases of $q=1.0$.
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{../results/plots/baseline_2026-01-23/research-article_aimrd_f/5y_all_cutoffs_full_lower_bound_non_lemmatized_.png}
    \caption{Behavior of different cutoff values and their corresponding excess word lists for PMC abstracts. Cutoffs are marked by color. Panel \textbf{a} shows the word list frequency $q$ as solid lines and the expected frequency $\hph$ as dashed lines. Panel \textbf{b} shows the lower bound estimates $\beta_{lb}$. For the highest cutoff values, both $q$ and $\hph$ are very close or equal to 1, leading to unreliable values with high fluctuation.}
    \label{fig:erratic}
\end{figure} 
\\
In optimizing $\beta_{lb}$, we also indirectly optimize for a word list that has $p_{LLM}$ very close to 1, because the more frequent a selection of words is in LLM-assisted text, the more useful it should be as LLM marker, therefore yielding higher values for $\beta_{lb}$.
Based on this reasoning, we call $\hbet$ an estimate for $\beta$, instead of its lower bound.
\\
The optimization was performed on yearly frequency data separately.
This means that the optimal cutoff for 2024 could be different to that of 2025.
For the main analysis, the optimal cutoff values for 2025 were used for all time-points to simplify procedure and have more easily comparable results.
The main analysis was performed on monthly data with the word lists optimized for the year 2025, instead of optimizing each month individually.
A possible approach with optimizing for monthly intervals of the data would be to take the majority optimum as the cutoff for all estimates.
The consequence would be the same: Some time intervals would be estimated with the optimal word list, while others wouldn't.

\section{Method validation}
\subsection{Standard error for LLM-use estimates}
To better judge the reliability of the estimates, we computed the standard error and performed sanity checks using simulated frequency data.
Since the LLM-use estimate $\hbet=\frac{q -  \hph}{1-\hph}$ depends on $\hph$ and $q$, we first need to determine their respective errors, before propagating them to obtain the error for $\hbet$.
We conceptualize $q$ as the empirical MLE (maximum likelihood estimate) for the binomial probability of an excess word to appear in a paper.
The binomial MLE is equal to the number of successes divided by the number of Bernoulli trials.
Here, we define a trial as a paper, and a success occurs if an excess word appears in the paper. 
As such, the MLE's variance is $Var(q)=\frac{q(1-q)}{n}$, with $n$ as the number of papers in the current month or year.
$\hph$ is the regression estimate of a binomial probability. 
Since $\hph$ is not an empirical estimate, the binomal MLE variance doesn't apply and we focus on the regression variance.
Technically, the data points that the regression was trained on are empirical estimates (since training was done on $q$ values in the time span before the release of ChatGPT).
To factor in this binomial maximum likelihood uncertainty, it could be included in the regression formula.
However, this was omitted for simplicity.
The regression variance for the design matrix $X$ (including only values of $q$ on which the regression is fit, corresponding to the time before the release of ChatGPT), predictor matrix $x$ (including all time-points that are to be predicted, with pre- and post-ChatGPT frequencies) and response vector $y$ is computed with the following formula:
\begin{equation}
    Var(\hph)= \hat{\sigma}^2 x(X^TX)^{-1}x^T + \hat{\sigma^2}
\end{equation}
with $\hat{b}=(X^TX)^{-1}X^Ty$ as the estimated regression coefficients\footnote{As per common convention, the estimated regression coefficients are normally denoted $\hbet$, which was changed to $\hat{b}$ to avoid confusion with the LLM-use estimate $\hbet$}
and $\hat{\sigma}^2=\frac{\|y-X\hat{b}\|^2}{n-2}$ the unbiased estimate of the noise variance, where $n$ refers to the number of papers, of which the number of predictors, 2, is subtracted.
$\hat{\sigma}^2x(X^TX)^{-1}x^T $ is the covariance matrix of the regression coefficients, specifying the uncertainty of the mean prediction. 
To this, an additional $\hat{\sigma}$-term is added to estimate how far the predicted values would have deviated from this mean.
\\
The variance of $\hbet$, as a nonlinear function of $q$ and $\hph$, is not trivial to determine.
We can apply the Delta method, which uses Taylor series approximation, to do so.
%comment: citation
We assume $q$ and $\hph$ to be the means of random variables $Q$ and $P$, respectively.
First, we approximate $\hbet((Q,P))$ around the mean $\mu=(q, \hph)$ with a first-order Taylor expansion:
\begin{equation}
    \hbet((Q,P)) \approx \hbet(\mu) + \nabla\hbet(\mu)^T((Q,P)-\mu)
\end{equation}
with gradient $\nabla$ as
\begin{equation}
    \nabla \hbet(\mu)= \left[ \frac{\partial\hbet}{\partial q}, \frac{\partial\hbet}{\partial \hph}\right]^T = \left[ \frac{1}{1-\hph}, \frac{q-1}{(1-\hph)^2}\right]^T 
\end{equation}
We can use this to compute the variance of $\hbet$:
\begin{equation}
    \begin{split}
        Var(\hbet) &= Var(\hbet(\mu) + \nabla\hbet(\mu)^T((Q,P)-\mu)\\
        &= Var(\hbet(\mu) + \nabla\hbet(\mu)^T(Q,P)-\nabla\hbet(\mu)^T\mu)\\
        &= Var(\hbet(\mu)^T(Q,P))\\
        &= \nabla\hbet(\mu)^T\Sigma\nabla\hbet(\mu)
    \end{split}
\end{equation}
We assume independence of the two random variables $Q$ and $P$, so that the covariance matrix $\Sigma$ only has the diagonal entries of the estimate variances $Var(q)$ and $Var(\hph)$ derived above. 
We excluded all estimates with a standard error of $Var(\hbet)\ge 0.025$.
Note that the previously introduced constraint on $\hph<0.999$ is not made obsolete by the introduction of bounds on the standard error, because in situations where $q=1.0$, the binomial error is $\frac{1(1-1)}{n}=0$, which propagates through to the error on $\hbet$.

\subsection{Sanity check simulation}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\linewidth]{../results/plots/misc/intuition_simulation_v2.png}
    \caption{Illustration of the sampling procedure for the creation of an artificial dataset to test the LLM-use estimate $\hbet$ for varying levels of $\beta$. The dataset is modeled by a matrix with $n$ columns corresponding to documents and $m$ rows corresponding to excess words. The binary matrix entries tell us whether a word $w_i$ appears in document $d_j$. The matrix on the left, $M_{pre}$, corresponds to the pre-ChatGPT word frequencies. Here, the binary entries for word $w_i$ are drawn from a binomial distribution with probability $\ph(w_i)$. This probability is drawn from a gamma distribution. The matrix on the right, $M_{post}$, models post-ChatGPT word frequencies, meaning that some of the documents were written by humans (indicated in blue), while a fraction indicated by $\beta$ were written with the help of LLMs (green). For the blue part of the matrix, the matrix entries for word $w_i$ are drawn from the same binomial distribution as in $M_{pre}$, with probability $\ph(w_i)$. For the green part, the probability $\ph(w_i)$ is increased by $\ph(w_i)\delta_i$ with $\delta_i \sim U(0.5,5)$.}
    \label{fig:intution_simulation}
\end{figure}
We assess the procedure of finding the optimal excess word list to get the best possible lower bound by performing the same analysis on simulated data with known $\pl$ and $\ph$ and varying $\beta$.
We simulate data for $n=100,000$ texts, approximating the number of papers per year in our sample.
In our simulation, there are $m=500$ words, all of which are treated as excess words.
We create two matrices, $M_{pre}$ and $M_{post}$, each with dimensions $(n,m)$, corresponding to data before and after the release of ChatGPT, respectively.
For $M_{pre}$, each word $w$ is randomly assigned a probability $\ph(w)$. 
The probabilities are sampled from a gamma distribution with shape parameter $2$ and scale parameter $0.02$.
Then, $n$ binomial samples are drawn with $\ph(w)$ as probability of success for each word.
For $M_{post}$, a fraction of $\beta$ texts are selected as being LLM-generated.
As our assumptions require that $\ph<\pl$, the probabilities for words to occur in LLM-assisted text are sampled in Bernoulli trials with probability $\pl(w)=\ph(w)(1+\delta)$, with $\delta \sim U(0.5,5)$.
In cases where $\ph(w)(1+\delta)>1$, $\delta$ was newly sampled until the resulting $\pl(w)$ was in the range $[0,1]$.
In the sampling process, $n\cdot \beta$ binomial samples are drawn with probability $\pl(w)$, and $n\cdot (1-\beta)$ are drawn with probability $\ph(w)$.
\\
For the following analyses, the word frequencies in $M_{pre}$ are treated as analogous to $\hph$, and those in $M_{post}$ as the observed frequencies $q$.
The words in $M_{pre}$ are ordered by frequency, and excess lists are formed of words with frequencies lower than those of several different cutoffs. 
For each excess list corresponding to one of the cutoffs, the LLM-use estimates $\Delta$ and $\hbet$ are computed.
The only difference w.r.t. the procedure on the real data is during variance computation: $M_{pre}$, as opposed to $\hph$, is not the result of a regression, but, similar to $q$, the result of a binomial sampling procedure. 
Thus, the variance for frequencies for $M_{pre}$ are computed as the variance of a binomial estimator, $Var(M_{pre}) = \frac{p(1-p)}{n_{trials}}$ with $p$ the observed frequency and $n_{trials}=(1-\beta)\cdot n$.
As with the real data, we exclude cutoffs yielding frequencies $\ge0.999$ for $M_{pre}$, as well as estimates with standard error $\ge0.025$.


\section{Section length}

%\begin{figure}[htbp]
%   \centering
%    \includegraphics[width=0.9\linewidth]{../results/plots/baseline_2026-01-23/section_length.png}
%    \caption{}
%    \label{fig:section_length}
%\end{figure}
One of the goals of this project was to compare the estimated LLM-use of different sections of the paper with each other.
This might seem like an unfair comparison: When using word occurrence as LLM-marker, longer texts are at an advantage - there is simply more space for words to appear in.
For example, if an LLM produced some word once every 500 words on average, it would appear in the majority of 1000-word sections, but only in about half of 250-word sections, despite all of them being written by an LLM.
Since the word counts are binary, there is no correction for section length in the estimate.
This is a further argument for using section specific word lists instead of single words.
Optimizing the excess word list for a given section could balance out the disadvantage of shorter text length.
The longer the word list, and the higher its aggregated frequency, the less likely it is for an LLM-generated text, however short, to not contain at least one of its words.
However, when authors use LLMs, it is likely that they focus their usage on specific paragraphs that they wish to rephrase.
A longer section contains more paragraphs that might warrant re-working.
We can thus slightly shift the question from \emph{How high is LLM-use in this section?} to \emph{How high is LLM-use in a random paragraph of this section?}
% By focusing on paragraphs instead of full sections, the section length can be controlled for. 
This was implemented by cropping each section sample to a length of 255 words, which corresponds to the median length of the abstracts\footnote{This was computed with the previous baseline, for the current baseline the median abstract length is 248 words}.
The cropped part was placed randomly within each section sample by randomly drawing a starting position ranging between the first and 255th-to-last words in the text.
Note that the LLM-use estimate based on the cropped sections can't be used to determine the full fraction of papers that were written with some LLM participation.
Since we are interested in identifying not only those papers that are wholly written by an LLM, using only one paragraph per section will underestimate the number of papers written with the help of LLMs, because this paragraph might not include the parts of the paper where LLMs were used. 
However, by comparing the results from the full and cropped sections, we can check whether LLMs are used universally across the entire paper or whether their use is concentrated on some paragraphs.

\section{Subgroup analysis}
%comment: add plot with number of paper per country (barplot for ind country counts, say 2017 to 2025, pie chart with entries "english-speaking", "non-english speaking", "unknown country")
Another point of interest is how LLM-use differs across subsets of the data.
It is, for instance, plausible to assume that majority English-speaking countries will show different usage patterns than countries where English is learned as a second language.
Likewise, there might be differences between academic disciplines, with some being quicker to adopt a new technology.
Previous studies typically reported higher usage estimates for research areas related to computer science (CS) \cite{Liang2024_paper, Liang2025}.
To compare these categories, we repeated the experiments on different subsets of papers.
\\\\
\emph{Countries}
\\
The most salient criterion to compare countries with each other when it comes to LLM-use is their majority native language.
Since English is the de facto lingua franca of academia, one of the key capabilities of LLMs is their ability to translate or proofread text from authors uncertain of their English proficiency.
As this use-case is irrelevant for native English speakers (NES), it stands to reason that LLM-usage would be smaller for NES countries.
\\
To categorize countries into NES and NNES (non-native English speakers), we used the \emph{Three circles of English model}\cite{kachru1985standards}, which sorts the United States, the United Kingdom and Ireland, Australia and New Zealand, South Africa, and the majority of Canada into the ``inner circle'', where English is the native language of the majority of inhabitants and primarily used in their day-to-day lives.
All other countries were classified as NNES. 
Additionally to comparing English to non-English speaking countries, we also analyzed the LLM-use of individual countries.
We selected the 18 countries with highest paper counts in the years 2017 to 2025.
The list of countries and their respective paper counts can be found in Appendix \ref{ap:countries}. 
\\
We originally planned to use the excess word lists that are optimal for each section with regards to the full dataset, as determined in the optimization procedure detailed in Section \ref{subsec:wordlist}. 
However, this lead to observations of $q=1.0$ for several years and countries (pre- and post-release of ChatGPT), which automatically means $\hbet=1.0$. %comment: put in appendix?
Due to time constraints, it was unfeasible to search for the optimal list for every country and section.
As a heuristic to approximate the search process, the list corresponding to the next-lower cutoff was selected.
For example, for the abstract, the optimal cutoff was $0.1$, so we selected the list corresponding to a cutoff value of $0.07$, which was the value directly preceding $0.1$.
By lowering the cutoff, we reduce the number of words in the excess word list and can thus regulate the observed frequencies.
This ``suboptimal'' cutoff also lead to problematic observations, because in many cases, $q$ was within the regression error of $\hph$, which is not ideal for any metric depending on the difference between the two.
To adjust, we moved the cutoff value one position further down, e.g. by going from $0.1$ to $0.05$ instead of $0.7$ for the abstract.
This was done for all sections and countries, not only those with problematic observations.
\\\\
\emph{Research disciplines}
\\
Papers were sorted into different research areas by the titles of journals in which they were published, in a procedure based on a paper by González-Márquez et al. \cite{gonzalez2024landscape}.
The research areas are subfields of the biomedical domain, for example radiology or nursing, that were specified in the previous work.
Journals were sorted into a category via keyword search with the category titles, and were assigned no label in case of no match.
This is a relatively crude but simple approach that ignores many potential matches, e.g., the (hypothetical) \emph{Journal of Neurology} would be assigned the label ``neurology'', while the \emph{Neurological Journal} would not. 
The number of matches could be improved by using fizzy matching or manually annotating.
In assigning labels, the keyword appearing latest in the title was given precedence. 
For example, the \emph{Journal of Cancer Biochemistry} would be sorted into the ``biochemistry'' category, while \emph{The Biochemistry of Cancer} would be assigned the label ``cancer''.
This procedure resulted in 23\% of papers being labeled.
Only labels with more than 5000 papers were selected for further analysis.
Using the optimal excess word lists to compute frequencies lead to similar problems as detailed above, which is why we also used the cutoff twice removed from the optimal one.
The main point of interest in comparing different academic disciplines with regard to their LLM-use are fields related to CS, because researchers in these areas are expected to adapt to new LLM technology more quickly.
The category of interest in PMC are journals related to computation.
However, the word frequencies for computation papers have a clear outlier, which distorts the prediction $\hph$ and thus leads to unreliable $\hbet$ estimates.
Because of this, the results are not discussed further in the main part, but can be found in Appendix \ref{ap:disciplines}.